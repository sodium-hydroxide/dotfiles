# # latexmkrc - project-specific settings

# # Always use pdflatex (can be changed to lualatex or xelatex if needed)
# $pdflatex = 'pdflatex -interaction=nonstopmode -file-line-error %O %S';

# # Set output directory for logs and aux files
# $out_dir = '.logs';

# # Set path to input files
# @default_files = ('main.tex');

# # Copy final PDF to main.pdf in the project root
# $pdf_mode = 1;

# # Hook to move final PDF from logs to project root after successful build
# $cleanup_includes_cus_dep_generated = 1;
# $success_cmd = "cp .logs/main.pdf ./main.pdf";

# # Ensure logs and aux files are not left scattered
# $clean_ext = 'aux bbl blg log out toc lof lot fls fdb_latexmk synctex.gz nav snm vrb';
