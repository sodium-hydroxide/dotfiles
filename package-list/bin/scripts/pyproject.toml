[project]
name = "scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
sysup = "scripts.sysup.sysup:main"

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"
